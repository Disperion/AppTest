name: Build APK (Мониторинг сервера HA)

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: 🧩 Checkout repository
        uses: actions/checkout@v4

      - name: ☕ Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y wget unzip openjdk-17-jdk python3 python3-pip git

      # Отключаем BuildKit на уровне окружения и демона Docker
      - name: 🧱 Disable Docker BuildKit globally
        run: |
          echo '{"features":{"buildkit":false}}' | sudo tee /etc/docker/daemon.json
          sudo systemctl restart docker
          echo "✅ BuildKit disabled globally"
        shell: bash

      - name: 💾 Cache Buildozer & SDK
        uses: actions/cache@v4
        with:
          path: |
            ~/.buildozer
            ~/.gradle
            ~/.android
            ~/android-sdk
          key: ${{ runner.os }}-buildozer-${{ hashFiles('buildozer.spec') }}
          restore-keys: |
            ${{ runner.os }}-buildozer-

      - name: 📦 Install Android SDK
        run: |
          mkdir -p "$HOME/android-sdk"
          cd "$HOME/android-sdk"
          wget -q https://dl.google.com/android/repository/commandlinetools-linux-11076708_latest.zip -O cmdline-tools.zip
          unzip -qo cmdline-tools.zip -d cmdline-tools
          mkdir -p cmdline-tools/latest
          mv -f cmdline-tools/cmdline-tools/* cmdline-tools/latest/ || true
          yes | cmdline-tools/latest/bin/sdkmanager --sdk_root=$HOME/android-sdk --licenses
          yes | cmdline-tools/latest/bin/sdkmanager --sdk_root=$HOME/android-sdk \
            "platform-tools" "platforms;android-33" "build-tools;36.0.0"

      - name: 🧰 Verify AIDL
        run: |
          if [ ! -f "$HOME/android-sdk/build-tools/36.0.0/aidl" ]; then
            echo "❌ AIDL not found — SDK installation failed"
            exit 1
          fi
          echo "✅ AIDL installed successfully"

      - name: 🐍 Build APK with Buildozer (Docker)
        env:
          DOCKER_BUILDKIT: 0
          BUILDKIT_PROGRESS: plain
        run: |
          echo "🚧 Building APK..."
          docker pull --disable-content-trust kivy/buildozer:latest
          docker run --rm \
            -e ANDROIDSDK="$HOME/android-sdk" \
            -v ${{ github.workspace }}:/app \
            -v $HOME/android-sdk:/root/.buildozer/android/platform/android-sdk \
            -v $HOME/.buildozer:/root/.buildozer \
            -w /app \
            --env DOCKER_BUILDKIT=0 \
            kivy/buildozer:latest \
            buildozer -v android debug

      - name: 🚀 Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: monitoring-ha-apk
          path: bin/*.apk
