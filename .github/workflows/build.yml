- name: 📦 Установка Android SDK
  run: |
    mkdir -p "$HOME/android-sdk"
    cd "$HOME/android-sdk"
    
    # Удаляем существующий файл
    rm -f cmdline-tools.zip
    
    # Скачиваем файл и добавляем вывод статуса
    wget -q https://dl.google.com/android/repository/commandlinetools-linux-latest.zip -O cmdline-tools.zip
    echo "Файл скачан. Размер файла: $(ls -lh cmdline-tools.zip)"
    
    # Извлекаем архив с отображением прогресса
    unzip -qo cmdline-tools.zip -d cmdline-tools
    ls -la cmdline-tools
    
    # Настройки переменных окружения
    mkdir -p cmdline-tools/latest
    mv -f cmdline-tools/cmdline-tools/* cmdline-tools/latest/ || true
    export ANDROID_SDK_ROOT=$HOME/android-sdk
    export PATH=$PATH:$ANDROID_SDK_ROOT/cmdline-tools/latest/bin
    chmod -R 755 "$ANDROID_SDK_ROOT"
    
    # Подтверждение лицензий
    yes | sdkmanager --sdk_root=$ANDROID_SDK_ROOT --licenses
    yes | sdkmanager --sdk_root=$ANDROID_SDK_ROOT \
      "platform-tools" "platforms;android-33" "build-tools;36.0.0"
