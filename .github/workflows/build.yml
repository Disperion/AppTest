name: Build APK (Мониторинг сервера HA)

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04  # Используем стабильную версию Ubuntu

    steps:
      - name: 🧩 Получение исходников из репозитория
        uses: actions/checkout@v4

      - name: ☕ Установка зависимостей
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            wget unzip openjdk-17-jdk python3 python3-pip git \
            libncurses5 libtinfo5

      - name: 🔬 Проверка версии Java
        run: |
          export JAVA_HOME=/usr/lib/jvm/java-17-openjdk-amd64
          $JAVA_HOME/bin/java -version

      - name: 💾 Кэширование Buildozer и Android SDK
        uses: actions/cache@v4
        with:
          path: |
            ~/.buildozer
            ~/.gradle
            ~/.android
            ~/android-sdk
